export const setTrip = (trip) => {
    return {
        type: "SET_TRIP",
        payload: trip
    }
};

export const setChat = (chat) => {
    return {
        type: "SET_CHAT",
        payload: chat
    }
};